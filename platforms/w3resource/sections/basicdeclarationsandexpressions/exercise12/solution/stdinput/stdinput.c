//
// Created by ivenpoker on 7/12/22.
//

#include <stdlib.h>
#include <stdio.h>
#include "stdinput_p.h"

SI_string_ct SI_nextLine() {

}

SI_string_ct SI_newStr(size_t size) {
    SI_string_ct str = (SI_string_t) malloc(sizeof(char) * (size + 1));
    if (isNull(str)) {

    }
}